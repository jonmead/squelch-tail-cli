# Core dependencies
# pygame-ce is the actively-maintained community fork with Python 3.14+ support
pygame-ce>=2.5.0

# UI widget layer
pygame-gui>=0.6.0

# Required for e-ink mode (Waveshare 2.13" driver converts pygame surface → PIL)
Pillow>=9.0.0

# Pixel array conversion for direct framebuffer rendering (LCD mode, /dev/fb1)
numpy>=1.24.0

# I2C communication for GT1151 touch controller (Waveshare 2.13" Touch HAT)
smbus2>=0.4.0

# Optional: Waveshare e-Paper HAT driver (e-ink mode only)
# Install on Raspberry Pi Zero:
#   pip install waveshare-epaper
# Or clone the official library:
#   git clone https://github.com/waveshare/e-Paper
#   pip install ./e-Paper/RaspberryPi_JetsonNano/python/
